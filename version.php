<?php

defined('MOODLE_INTERNAL') || die();

// if this doesn't work, change the two versions to be the same as the other plugins in your site
$plugin->version   = 2024000000;     // The current plugin version (Date: YYYYMMDDXX)
$plugin->requires  = 2024000000;        // Requires this Totara version
$plugin->component = 'local_todo';         // Full name of the plugin (used for diagnostics)
